import twitter

class TwitterInterface:
    def __init__(self, data_interface):
        self.data_interface = data_interface
        self.api = connect_to_twitter()

    def connect_to_twitter(self):
        consumer_secret =
        access_token_key =
        access_token_secret = 
